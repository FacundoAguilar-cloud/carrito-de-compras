package com.demo.app.demo_msvc_app.enums;
//manejamos de forma simple los distintos estados de la orden
public enum OrderStatus {
    PENDING,
    PROCESSING,
    SHIPPED,
    DELIVERED,
    CANCELLED
}
